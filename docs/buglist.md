# Buglist

| #  | 问题                                                      | 状态                                 | 修复 |
|----|---------------------------------------------------------|------------------------------------|------|
| 1  | 程序未做单例限制，多开导致时间重复计算                                     | ✅                                  | 命名互斥体 |
| 2  | 分类点击无反应                                                 | ✅                                  | `main_window.set_repo` 修复 |
| 3  | 添加应用无反应                                                 | ✅                                  | 新建分类自动保存后再分配 |
| 4  | 颜色对话框按钮因主题不可见                                           | ✅                                  | `QColorDialog` / `QFileDialog` 专用 QSS |
| 5  | 应用图标仍是默认占位图                                             | ✅                                  | 从 DB 查 `executable_path` 提取真实图标 |
| 6  | 托盘右键菜单不可见                                               | ✅                                  | `QMenu` QSS |
| 7  | 开机自启设置                                                  | ✅                                  | `auto_start.py` 注册表读写 |
| 8  | 自定义数据清理                                                 | ✅                                  | 设置页按月选择 + 手动触发 |
| 9  | 复选框看不清                                                  | ✅                                  | `QCheckBox` QSS |
| 10 | 日历格子看不清                                                 | ✅                                  | Notion 风格 QPainter 网格，今日紫色圆高亮 |
| 11 | 字段删除按钮失效                                                | ✅                                  | `get_task_by_id` joinedload 预加载 |
| 12 | 任务详情无关闭按钮                                               | ✅                                  | 模态框右上角 ✕ |
| 13 | 未适配多尺度                                                  | ✅                                  | 弹性布局 + 网格拉伸 |
| 14 | 添加任务的 + 号消失                                             | ✅                                  | hover 单元格右上角显示 + |
| 15 | 创建任务弹出卡片取代侧栏                                            | ✅                                  | `TaskCreatePopup` 浮动卡片 |
| 16 | 多任务条重叠不换行                                               | ✅                                  | 堆叠算法自动分配行号，最多 10 行 |
| 17 | 任务条颜色突兀                                                 | ✅                                  | 改为简洁边框风格：`#25253a` 背景 + `3a3a50` 边框 + 紫色左侧竖条 |
| 18 | 任务详情在屏幕中央弹出                                             | ✅                                  | `TaskDetailDialog` 模态框 580×620 |
| 19 | 详情页字段无边框                                                | ✅                                  | 每个字段独立框 `1px solid #3a3a50`，hover 变色 |
| 20 | AI 自动分类使用 DeepSeek API 报错                               | ✅                                  | 移除 OpenAI 专有 `response_format` 参数，纯文本 prompt 引导 JSON 输出 |
| 21 | 应用图标展示还是有问题（strip_ext 后拿改名去查库，查不到 exe_path）             | ✅                                  | 统一用 raw_name 查库+查进程，disp_name 只用于显示 |
| 22 | 仪表盘今日活跃时长等显示空白                                          | ✅                                  | 硬编码 `color: #ffffff` + objectName 未匹配 `cardValue` 主题选择器，亮色下白字白底；改为 `cardValue`，颜色由主题统一管理 |
| 23 | 仪表盘今日活跃时长、应用数、当前会话三个值一样（`_set_card` 全找到第一个 `cardValue`） | ✅                                  | 改用 `self._card_values[title]` 字典直接引用，不再通过 QSS 名称查找 |
| 24 | 已分类但分类占比空白                                              | ✅                                  | 异常被 `except: pass` 吞没不显示；加固异常日志，路径各异时间段也显示文案 |
| 25 | AI 自动分类报错 no module named openai                        | ✅                                  | `llm_client.py` 加自动安装：import 失败时自动 `pip install openai`，无需手动处理 |
| 26 | 分类内部应用展示过窄                                              | ✅                                  | 左侧改为紧凑彩色图标按钮选择器（40px 高），右侧全宽展示编辑面板 + 应用列表 |
| 27 | 图标选择面板不可滚动                                              | ✅                                  | 图标网格放入 QScrollArea，固定高 160px，可上下滑动 |
| 28 | 应用列表项高度过窄显示不全                                           | ✅                                  | 显式设置 `item.setSizeHint(QSize(200, 36))`，不再依赖自动布局 |
| 29 | 编辑分类保存时闪烁                                               | ✅                                  | 保存/删除不再全量重建按钮，仅更新/删除单个按钮 |
| 30 | 编辑分类颜色不对应                                               | ✅                                  | `_edit_category` 未设置 `_selected_color_edit`，保存时总是回退到 `#7c5cfc` |
| 31 | 仪表盘当前会话文字显示不全（font-size 26px 超出卡片宽度）                    | ✅                                  | 主题中 `cardValue` 字号从 26px 降至 22px |
| 32 | 周视图左右切换触发除 0 异常                                         | ✅ | `_max_value` 全 0 时 `or 1` 兜底 |
| 33 | 日历格子右上角加号不可见（仅 hover 显示）                                | ✅ | 改为常态显示：默认灰色 `#2a2a44` 底 + `#808098` 字，hover 变亮 `#4a4a6a` + 白字 |
| 34 | 加号 hover 无动画                                            | ✅ | `QVariantAnimation` 150ms 颜色渐变（灰 → 紫），移出淡出 |
| 35 | 创建任务卡片位置不在日历页正中                                         | ✅ | 用 `mapToGlobal(rect().center())` 计算屏幕全局坐标，Popup 居中显示 |
| 36 | 创建标题弹窗文本框上方有圆角矩形框                                       | ✅ | QSS 选择器从 `QFrame` 改为 `QFrame#taskPopup`，避免误匹配子控件 |
| 37 | 字段删除/添加不即时刷新                                            | ✅ | `_rebuild_fields` 重新查询任务（`get_task_by_id`），不再用 ORM 缓存 |
| 38 | 字段文本框过大                                                 | ✅ | 统一 36px 高，多行自动滚动，QScrollArea 上下滚轮 |
| 39 | 删除任务后评论有残余                                              | ✅ | `delete_task` 改用 ORM `s.delete(task)` 触发 cascade 连带删除字段和评论 |    |
| 40 | 任务内删除字段没反应                                              | ✅ | lambda 元组表达式改为独立 `_delete_field` 方法，带异常日志 |
| 41 | 字段大小不统一                                                 | ✅ | 字段 frame 固定 40px 高，文本框固定 30px，所有字段一致 |
| 42 | 文本框应随内容行数自适应增高（Notion 风格）                               | ✅ | `_auto_resize_edit` 按 document 高度自动调整（30~160px），输入时实时变化 |
| 43 | 字段初始占满空间                                                | ✅ | 字段按 `document().size()` 实际高度自适应，空字段统一 30px |
| 44 | 字段与删除按钮间距过大                                             | ✅ | 布局末尾 `addStretch(1)`，剩余空间挤到底部，中间无空隙 |
| 45 | 字段内滚轮方向反了                                               | ✅ | `_FieldEdit` 子类忽略滚轮事件，统一交给对话框滚动区域 |
| 46 | 退出编辑后字段尺寸回退                                             | ✅ | `resizeEvent` + `_resize_all_fields` 布局变化后重新测量所有字段 |
| 47 | 日历加号不居中 + 今日圆不包裹日期 | ✅ | + 号用线段绘制精确居中；日期文字与圆形同一矩形绘制 |
| 48 | 日期选择弹窗部分日期不可见/显示省略号 | ✅ | 彻底弃用 QCalendarWidget，自研 QPainter 月历（`date_picker_dialog.py`），渲染与日历页一致 |
| 49 | 启动时窗口跑到左上角 | ✅ | `_center_on_screen` 显示前显式居中 |
| 50 | 跨月任务下月不显示、不可点击 | ✅ | 查询改为"与当月有交集"；网格存完整日期并按月裁剪渲染/命中 |
| 51 | 跨行任务第二行只有占位无内容 | ✅ | 任务条按行分段绘制，跨行任务每行都显示连续条 |
| 52 | 跨行任务第二行应显示标题 | ✅ | 每段都绘制标题，跨行时每行第一格带任务名（Notion 风格） |
| 53 | 提醒下拉框（普通/定时）文字与背景同色不可见 | ✅ | 提取共享 `StyledCombo`（弹出注入暗色样式），提醒下拉框改用，与主题下拉框同款 |